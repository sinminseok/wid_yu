import 'package:flutter/material.dart';

class YoungFunningContectView extends StatelessWidget {
  const YoungFunningContectView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
