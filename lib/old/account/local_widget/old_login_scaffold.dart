import 'package:flutter/material.dart';

class OldLoginScaffold extends StatelessWidget {
  const OldLoginScaffold({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
